[project]
name = "booster"
version = "0.1.0"
description = "CLI script to download media from boosty.to"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9.0",
    "aiofiles>=23.2.0",
    "tqdm>=4.66.0",
]
